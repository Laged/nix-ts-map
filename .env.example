# nix-ts-map Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to git - it's already in .gitignore

# This file is loaded automatically by scripts/with-dotenv.sh during local development.
# Copy it to .env and override values as needed.

# ClickHouse Database Configuration
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DATABASE=default

# Scraper Configuration
# Bounding box format: "minLat,minLon,maxLat,maxLon"
# Default: Finland area
FINLAND_BOUNDS=60,20,70,30

# Scraping interval in seconds
SCRAPE_INTERVAL_SECONDS=60

# OpenSky Network API (Optional)
# OpenSky allows anonymous access, but authenticated users get higher rate limits
# Register at https://opensky-network.org/accounts/login/ to get credentials
OPENSKY_USERNAME=
OPENSKY_PASSWORD=

# GraphQL Server Configuration
PORT=4000
HOST=0.0.0.0

# Frontend Configuration
VITE_GRAPHQL_URL=http://localhost:4000/graphql
VITE_MAPBOX_TOKEN=your_mapbox_token_here
