/**
 * @map/shared - Shared TypeScript types for the flight tracking platform
 * 
 * This package provides the single source of truth for data structures
 * used across the entire application stack (scraper, database, API, frontend).
 */

export * from './types';

